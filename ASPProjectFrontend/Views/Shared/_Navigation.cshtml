@using System.Security.Claims
@inject IHttpContextAccessor HttpContextAccessor
@{
    var auth = HttpContextAccessor.HttpContext.User.Identity.IsAuthenticated;
    var admin = HttpContextAccessor.HttpContext.User.IsInRole("Admin");
    var email = HttpContextAccessor.HttpContext.User.Claims.FirstOrDefault(c => c.Type == ClaimTypes.Email)?.Value;
}
<nav class="navbar navbar-expand-sm navbar-toggleable-sm box-shadow mb-3 alt-bg-color">
    <div class="container-fluid alt-bg-color">
        <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">ASPProjectFrontend</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul class="navbar-nav flex-grow-1 alt-bg-color">
                <li class="nav-item">
                    <a class="nav-link text-color-red" asp-area="" asp-controller="Home" asp-action="Index">Store</a>
                </li>
              <li class="nav-item">
			        <a class="nav-link text-color-red" asp-area="" asp-controller="ShoppingCart" asp-action="Index">Cart</a>
		        </li>
            </ul>
            <ul class="navbar-nav justify-content-end">
                <li class="nav-item">
                    @if(!auth){
                        <a asp-action="SignInWithGoogle" asp-controller="Authentication" class="btn accent-green text-white">
                            <i class="fa-brands fa-google"></i>
                            Login with Google
                        </a>
                    }
                    else
                    {
                        <a asp-action="Logout" asp-controller="Account" class="btn btn-danger">Logout</a>
                    }

                </li>
            </ul>
            
        </div>
    </div>
</nav>